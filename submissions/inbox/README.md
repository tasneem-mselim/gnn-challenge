# Submissions Inbox

Store submission files in:

```text
submissions/inbox/<team>/<run_id>/
```

Required files per submission:

- `predictions.csv.enc` (encrypted `id`,`y_pred` CSV payload)
- `metadata.json`

Use root `CONTRIBUTING.md` as the canonical submission guide.
